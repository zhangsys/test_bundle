# This is a Databricks asset bundle definition for test_lola_1106.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: test_lola_1106
  uuid: b9f9dab6-db47-4a19-b502-ef2f0fc415ee

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-60d2544c-14e3.cloud.databricks.com
    variables:
      catalog: bundle_test
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-60d2544c-14e3.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/lolayqzhang@tencent.com to make sure we only have a single copy.
      root_path: /Workspace/Users/lolayqzhang@tencent.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: bundle_test
      schema: prod
    permissions:
      - user_name: lolayqzhang@tencent.com
        level: CAN_MANAGE
