# The main pipeline for test_lola_1106

resources:
  pipelines:
    test_lola_1106_etl:
      name: test_lola_1106_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/test_lola_1106_etl"

      libraries:
        - glob:
            include: ../src/test_lola_1106_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
